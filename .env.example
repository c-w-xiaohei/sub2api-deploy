# Non-authoritative notice: deployment configuration is Pulumi's four
# structured objects (edge, sites, edgeSecrets, and siteSecrets). This file is
# intentionally not a flat-variable configuration interface and is not loaded.
